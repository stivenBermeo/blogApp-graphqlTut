{
  "name": "blog-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start:dev": "nodemon --watch './**/*.ts' --exec 'ts-node' src/index.ts",
    "start:postgres": "docker run -itd -e POSTGRES_USER=root -e POSTGRES_PASSWORD=root1234 -p 5432:5432 -v /private/var/lib/postgresql/data --name postgresql postgres",
    "test": "echo \"Error: no test specified\" && exit 1",
    "docker:build:db": "docker build -t db ./db",
    "docker:run:db": "docker run --name psd -e POSTGRES_PASSWORD=fred -p 5432:5432 -d postgres",
    "docker:pull:pgadmin": "docker pull dpage/pgadmin4:latest",
    "docker:run:pgadmin": "docker run --name pgadmin-blogApp -p 5051:80 -e 'PGADMIN_DEFAULT_EMAIL=root@gmail.com' -e 'PGADMIN_DEFAULT_PASSWORD=baeldung' -d dpage/pgadmin4"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^4.3.1",
    "apollo-server": "^3.10.1",
    "graphql": "^16.5.0"
  },
  "devDependencies": {
    "prisma": "^4.3.1",
    "ts-node-dev": "^2.0.0",
    "typescript": "^4.7.4"
  }
}
